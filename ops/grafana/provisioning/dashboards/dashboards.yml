apiVersion: 1

providers:
  - name: observable-shop
    orgId: 1
    type: file
    folder: Observability
    folderUid: observability
    disableDelition: false
    allowUiUpdates: true
    updateIntervalSeconds: 30

    options:
      path: /var/lib/grafana/dashboards
      foldersFromFileStructure: true
